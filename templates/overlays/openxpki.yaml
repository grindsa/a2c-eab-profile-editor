extends: kid_profiles.template.yaml
description: Overlay hints for OpenXPKI CA handler fields.

sections:
  - id: cahandler
    fields_add:
      - name: cert_profile_name
        type: string_or_list
        help: OpenXPKI certificate profile name(s).
